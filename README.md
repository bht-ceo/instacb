# instacb

$pkg install && pkg update

$pkg upgrade

$pkg install openssl-tool

$ pkg install curl

$ pkg install git

$ git clone https://github.com/bht-ceo/instacb

$ cd instacb

$ chmod +x instacb

$ termux-wake-lock

$ bash instacb
